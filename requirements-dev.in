# Ferramentas de QA, Segurança e Testes
bandit
pip-audit>=2.4.0,<3.0.0
ruff
pytest>=7.0.0,<8.0.0
semgrep>=1.0.0; sys_platform != 'win32'

# Compatibilidade para versões antigas do Python
importlib-metadata; python_version < "3.10"
-c requirements.txt

# Referência ao arquivo principal (opcional, mas recomendado para garantir compatibilidade)
-c requirements.txt
